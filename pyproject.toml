[project]
name = "airflow-helm-dags"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "leobellap",email = "leobellap@gmail.com"},
]
dependencies = ["apache-airflow-task-sdk>=1.3.1"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
